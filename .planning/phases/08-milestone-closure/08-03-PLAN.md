---
phase: 08-milestone-closure
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/scheduler/jobs/circuit_breaker.py
autonomous: true
requirements: [PUBL-01]

must_haves:
  truths:
    - "src/app/scheduler/jobs/circuit_breaker.py no longer exists in the working tree"
    - "No Python file in the codebase imports from app.scheduler.jobs.circuit_breaker"
    - "The production circuit breaker service at src/app/services/circuit_breaker.py is untouched"
    - "All existing tests pass after deletion"
  artifacts:
    - path: "src/app/scheduler/jobs/circuit_breaker.py"
      provides: "DELETED — orphaned file confirmed unreachable; audit gap INT-01 closed"
  key_links:
    - from: "Production code (all services)"
      to: "app.services.circuit_breaker"
      via: "No change — existing imports remain valid"
      pattern: "from app.services.circuit_breaker import"
---

<objective>
Delete the orphaned file src/app/scheduler/jobs/circuit_breaker.py which is confirmed unreachable — no production file imports from it, and it is missing the Phase 7 methods (is_daily_halted, clear_daily_halt) that the production circuit_breaker.py at app.services.circuit_breaker implements.

Purpose: Audit gap INT-01 requires deletion of this stale duplicate. Its presence creates confusion (two "circuit_breaker.py" files), and it could mislead future contributors. Deletion carries zero risk since it is unreachable code.

Output: File deleted from working tree and git index.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned circuit_breaker.py and verify no imports reference it</name>
  <files>src/app/scheduler/jobs/circuit_breaker.py</files>
  <action>
Perform the following steps in order:

Step 1 — Confirm the file exists before deletion:
Run: ls src/app/scheduler/jobs/circuit_breaker.py
If it does not exist, the task is already complete — skip to Step 4.

Step 2 — Confirm no imports reference it:
Run: grep -r "scheduler.jobs.circuit_breaker" src/ tests/ 2>/dev/null
Expected result: zero matches. If any matches are found, STOP and report — do not delete until imports are identified and resolved.

Step 3 — Delete using git rm:
Run: git rm src/app/scheduler/jobs/circuit_breaker.py
This removes the file from both the working tree and the git index.

Step 4 — Verify the production circuit breaker is intact:
Run: ls src/app/services/circuit_breaker.py
Expected: file still exists.

Step 5 — Verify all tests still pass:
Run: uv run pytest tests/ -v -m "not e2e"
Expected: all non-e2e tests pass (0 failures). The orphaned file was never imported by any test, so deletion cannot break tests.

Do NOT delete any other file. Do NOT modify src/app/services/circuit_breaker.py. Do NOT update any imports (none reference the deleted file).
  </action>
  <verify>
    <automated>cd /Users/jesusalbino/Projects/content-creation && test ! -f src/app/scheduler/jobs/circuit_breaker.py && test -f src/app/services/circuit_breaker.py && echo "Orphan deleted, production file intact" && uv run pytest tests/ -v -m "not e2e" --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>
src/app/scheduler/jobs/circuit_breaker.py does not exist. src/app/services/circuit_breaker.py still exists. All non-e2e tests pass. git status shows the deleted file as "deleted" in the index (git rm staged it).
  </done>
</task>

</tasks>

<verification>
After task completes:

1. File gone: `test ! -f src/app/scheduler/jobs/circuit_breaker.py` exits 0
2. Production file intact: `test -f src/app/services/circuit_breaker.py` exits 0
3. No dangling imports: `grep -r "scheduler.jobs.circuit_breaker" src/ tests/` returns nothing
4. Tests pass: `uv run pytest tests/ -m "not e2e" -q` exits 0 with no failures
5. Git index: `git status src/app/scheduler/jobs/circuit_breaker.py` shows "deleted"
</verification>

<success_criteria>
- Orphaned file is deleted from working tree and git index
- Production circuit_breaker at app.services.circuit_breaker is unchanged
- All existing smoke tests continue to pass
- Audit gap INT-01 is closed: no stale duplicate circuit_breaker.py in the scheduler/jobs directory
</success_criteria>

<output>
After completion, create `.planning/phases/08-milestone-closure/08-03-SUMMARY.md`
</output>
