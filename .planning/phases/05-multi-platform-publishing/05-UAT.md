---
status: complete
phase: 05-multi-platform-publishing
source: [05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md, 05-04-SUMMARY.md, 05-05-SUMMARY.md]
started: 2026-02-28T00:00:00Z
updated: 2026-02-28T00:05:00Z
---

## Current Test

## Current Test

[testing complete]

## Tests

### 1. Smoke Tests Pass (13/13)
expected: Running `uv run pytest tests/test_phase05_smoke.py -v` completes with 13 passed, 0 failed.
result: pass

### 2. Migration SQL Uses external_post_id
expected: Opening `migrations/0005_publishing.sql`, the publish_events table definition contains `external_post_id    text,` — NOT `ayrshare_post_id`. The comments explain it holds platform-native post IDs (YouTube video ID, Instagram media ID, Facebook video ID).
result: pass

### 3. Settings Has Direct Platform Credentials (No Ayrshare)
expected: Opening `src/app/settings.py`, the Publishing section has 7 new fields: `instagram_access_token`, `instagram_business_account_id`, `facebook_access_token`, `facebook_page_id`, `youtube_client_id`, `youtube_client_secret`, `youtube_refresh_token`. There is NO `ayrshare_api_key` field. All 7 are required with no defaults.
result: pass

### 4. PublishingService Has Per-Platform Publish Methods
expected: Opening `src/app/services/publishing.py`, the class has `_publish_youtube`, `_publish_instagram`, `_publish_facebook` (each with @retry decorator) and a `publish()` dispatcher that routes to them. There is no `_post()` method. `MANUAL_PLATFORMS = {"tiktok"}` and `AUTO_PLATFORMS = {"instagram", "facebook", "youtube"}` are defined at module level.
result: pass

### 5. Approval Message Shows 4 Platform Copy Variants
expected: The approval Telegram message caption includes a "COPY POR PLATAFORMA" section with emoji-labeled blocks for TikTok (🎵), Instagram (📷), Facebook (🟦), and YouTube (▶️). This is generated by `send_approval_message()` in `telegram.py` and the platform variants are persisted to `content_history` columns.
result: pass

### 6. Confirmation Message Shows 3 Scheduled Times + TikTok Manual Block
expected: After the creator taps Approve, `send_publish_confirmation()` sends a message with scheduled times for Instagram, Facebook, and YouTube only (no TikTok time). Below a `---` separator, a TikTok manual-posting block appears: `🎵 TikTok — Publicación manual:` followed by `Video: {url}` and `Copy: {first 300 chars}...`
result: pass

### 7. schedule_platform_publishes Only Schedules 3 Jobs (Skips TikTok)
expected: In `src/app/services/publishing.py`, the `schedule_platform_publishes()` function iterates over `{"instagram", "facebook", "youtube"}` (not tiktok). The returned `scheduled_times` dict contains only those 3 platforms. No DateTrigger job is created for TikTok.
result: pass

### 8. verify_publish_job Uses external_post_id and Dispatches by Platform
expected: In `src/app/scheduler/jobs/publish_verify.py`, the function signature is `verify_publish_job(content_history_id, platform, external_post_id)`. Inside, it calls `PublishingService().get_post_status(platform, external_post_id)` — passing platform as first argument. `SUCCESS_STATUSES` includes `"verified"`.
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
