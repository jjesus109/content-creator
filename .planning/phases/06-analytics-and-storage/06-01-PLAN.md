---
phase: 06-analytics-and-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0006_analytics.sql
  - src/app/settings.py
autonomous: true
requirements:
  - ANLX-01
  - ANLX-04

must_haves:
  truths:
    - "The platform_metrics table exists with all required columns and constraints"
    - "The content_history table has storage lifecycle columns (storage_status, r2_key, is_viral, is_eternal)"
    - "Settings exposes R2 credentials (r2_account_id, r2_access_key_id, r2_secret_access_key, r2_bucket_name)"
    - "Settings exposes TikTok credentials (tiktok_access_token, tiktok_refresh_token) with Optional default"
  artifacts:
    - path: "migrations/0006_analytics.sql"
      provides: "platform_metrics table + content_history lifecycle columns"
      contains: "CREATE TABLE IF NOT EXISTS platform_metrics"
    - path: "src/app/settings.py"
      provides: "R2 and TikTok settings fields"
      contains: "r2_account_id"
  key_links:
    - from: "migrations/0006_analytics.sql"
      to: "content_history"
      via: "ALTER TABLE content_history ADD COLUMN IF NOT EXISTS storage_status"
      pattern: "ADD COLUMN IF NOT EXISTS storage_status"
    - from: "src/app/settings.py"
      to: "r2_account_id"
      via: "Pydantic BaseSettings field"
      pattern: "r2_account_id"
---

<objective>
Write the database migration and extend Settings with the new credentials required by Phase 6.

Purpose: All Phase 6 jobs and services read from new DB columns and new env vars. Everything downstream depends on these two files existing first.
Output: `migrations/0006_analytics.sql` + extended `src/app/settings.py`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-and-storage/06-CONTEXT.md
@.planning/phases/06-analytics-and-storage/06-RESEARCH.md
@src/app/settings.py
@migrations/0005_publishing.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0006_analytics.sql</name>
  <files>migrations/0006_analytics.sql</files>
  <action>
Create `migrations/0006_analytics.sql` with the following SQL verbatim (do not paraphrase — use exact column names and constraints as specified):

```sql
-- Migration 0006: Analytics and Storage — Phase 6
-- platform_metrics: per-platform per-video metrics row inserted by 48h harvest job
-- content_history additions: storage_status, r2_key, deletion flags, viral/eternal flags

CREATE TABLE IF NOT EXISTS platform_metrics (
    id                  uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    created_at          timestamptz DEFAULT now() NOT NULL,
    harvested_at        timestamptz NOT NULL,
    content_history_id  uuid NOT NULL REFERENCES content_history(id),
    platform            text NOT NULL CHECK (platform IN ('youtube', 'instagram', 'tiktok', 'facebook')),
    external_post_id    text,
    views               integer,
    likes               integer,
    shares              integer,
    comments            integer,
    reach               integer,
    saves               integer,
    virality_alerted_at timestamptz
);

CREATE INDEX IF NOT EXISTS platform_metrics_content_history_idx
    ON platform_metrics(content_history_id);

CREATE INDEX IF NOT EXISTS platform_metrics_platform_harvested_idx
    ON platform_metrics(platform, harvested_at);

ALTER TABLE content_history
    ADD COLUMN IF NOT EXISTS storage_status       text DEFAULT 'hot'
        CHECK (storage_status IN ('hot', 'warm', 'pending_deletion', 'deleted', 'exempt')),
    ADD COLUMN IF NOT EXISTS r2_key               text,
    ADD COLUMN IF NOT EXISTS storage_tier_set_at  timestamptz,
    ADD COLUMN IF NOT EXISTS deletion_requested_at timestamptz,
    ADD COLUMN IF NOT EXISTS is_viral             boolean DEFAULT false,
    ADD COLUMN IF NOT EXISTS is_eternal           boolean DEFAULT false;
```

Include a comment header: `-- Migration 0006: Analytics and Storage — Phase 6`.
Do NOT add a UNIQUE constraint on (content_history_id, platform) — the harvest job inserts one row per platform per video and idempotency is handled via job ID deduplication.
  </action>
  <verify>Read the file and confirm: platform_metrics table present, all 13 columns present (id, created_at, harvested_at, content_history_id, platform, external_post_id, views, likes, shares, comments, reach, saves, virality_alerted_at), two indexes, six ALTER TABLE columns on content_history (storage_status with CHECK, r2_key, storage_tier_set_at, deletion_requested_at, is_viral, is_eternal).</verify>
  <done>File exists and contains all required SQL without UNIQUE constraint on (content_history_id, platform).</done>
</task>

<task type="auto">
  <name>Task 2: Extend Settings with R2 and TikTok credentials</name>
  <files>src/app/settings.py</files>
  <action>
Read the full current `src/app/settings.py`. Then add the following field blocks at the end of the Settings class, before the closing `@lru_cache` function:

**Block 1 — Cloudflare R2 (new, after the existing audience timezone block):**
```python
    # Cloudflare R2 warm storage (ANLX-04)
    # Credentials from R2 dashboard → Manage API Tokens → Create Token (Object Read & Write)
    r2_account_id: str = ""             # Cloudflare account ID (found in right sidebar of R2 console)
    r2_access_key_id: str = ""          # R2 API token Access Key ID
    r2_secret_access_key: str = ""      # R2 API token Secret Access Key
    r2_bucket_name: str = "content-warm-storage"   # R2 bucket name
```

Use empty-string defaults for R2 fields so the service starts without R2 configured. The lifecycle service will check `if not settings.r2_account_id` and skip R2 operations gracefully.

**Block 2 — TikTok OAuth (new, after R2 block):**
```python
    # TikTok Display API OAuth 2.0 tokens (ANLX-01)
    # Creator must authorize once via /auth/tiktok FastAPI route
    # access_token and refresh_token stored here after first authorization
    tiktok_access_token: str = ""       # Display API user access token (expires in 24h, refresh via refresh_token)
    tiktok_refresh_token: str = ""      # Refresh token (valid for 365 days)
```

Use empty-string defaults for TikTok fields. The metrics harvester will check `if not settings.tiktok_access_token` and skip TikTok harvest with a logged warning (graceful degradation per CONTEXT.md).

Preserve all existing fields exactly. Do not move or reformat existing lines.
  </action>
  <verify>Run `python -c "from app.settings import Settings; s = Settings.model_construct(); print(s.model_fields.keys())"` from `src/` to confirm new fields are importable. Alternatively, confirm the file contains `r2_account_id`, `r2_access_key_id`, `r2_secret_access_key`, `r2_bucket_name`, `tiktok_access_token`, `tiktok_refresh_token`.</verify>
  <done>Settings class has all 6 new fields with empty-string defaults. Existing fields are unchanged. `get_settings()` still works with the current .env file (no new required fields — all have defaults).</done>
</task>

</tasks>

<verification>
1. `cat migrations/0006_analytics.sql` — shows CREATE TABLE platform_metrics and 6 ALTER TABLE columns.
2. `grep -n "r2_account_id\|tiktok_access_token" src/app/settings.py` — both fields present.
3. `cd src && python -c "from app.settings import get_settings; s = get_settings(); print(s.r2_account_id)"` — prints empty string without error.
</verification>

<success_criteria>
- migration 0006_analytics.sql is syntactically valid SQL with all 13 platform_metrics columns and 6 content_history additions
- Settings loads with no ValidationError when R2/TikTok env vars are absent (all have defaults)
</success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-storage/06-01-SUMMARY.md`
</output>
