---
phase: 07-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0007_hardening.sql
  - src/app/services/circuit_breaker.py
  - src/app/scheduler/jobs/cb_reset.py
  - src/app/telegram/handlers/resume_flow.py
  - src/app/telegram/app.py
  - src/app/scheduler/jobs/daily_pipeline.py

autonomous: true
requirements:
  - INFRA-04
  - SCRTY-02

must_haves:
  truths:
    - "Migration 0007 adds daily_trip_count and daily_halted_at to circuit_breaker_state, and adds approval_timeout to content_history video_status CHECK constraint"
    - "When daily_pipeline_job is skipped 3 times in a calendar day due to circuit breaker trip, a halt Telegram alert fires with /resume instructions — the pipeline does not loop"
    - "Creator types /resume in bot chat (creator ID only), daily halt state clears, pipeline retries immediately"
    - "midnight_reset clears daily_trip_count and daily_halted_at alongside existing weekly_trip_count logic"
    - "/resume handler is registered in build_telegram_app() — same pattern as mood_flow handlers"
  artifacts:
    - path: "migrations/0007_hardening.sql"
      provides: "daily_trip_count + daily_halted_at on circuit_breaker_state, approval_timeout in video_status CHECK"
      contains: "daily_trip_count"
    - path: "src/app/services/circuit_breaker.py"
      provides: "Daily halt logic (3 trips/day alert with /resume instructions)"
      contains: "daily_trip_count"
    - path: "src/app/telegram/handlers/resume_flow.py"
      provides: "/resume CommandHandler — clears daily halt, triggers pipeline rerun"
      exports: ["handle_resume", "register_resume_handler"]
    - path: "src/app/telegram/app.py"
      provides: "register_resume_handler wired into build_telegram_app"
  key_links:
    - from: "src/app/services/circuit_breaker.py (_trip)"
      to: "circuit_breaker_state.daily_trip_count"
      via: "increment daily_trip_count on every trip; check >= 3 for halt condition"
      pattern: "daily_trip_count"
    - from: "src/app/telegram/handlers/resume_flow.py (handle_resume)"
      to: "circuit_breaker_state table"
      via: "update daily_trip_count=0, daily_halted_at=NULL"
      pattern: "daily_trip_count.*0"
    - from: "src/app/telegram/app.py (build_telegram_app)"
      to: "resume_flow.register_resume_handler"
      via: "function call at app build time"
      pattern: "register_resume_handler"
---

<objective>
Add the database migration for daily halt state, extend CircuitBreakerService with 3-trips-per-day halt logic, and implement the /resume Telegram command handler.

Purpose: Gives the autonomous system a hard stop when the circuit breaker fires three times in a day, with a clear manual recovery path (/resume command) so the creator can resume without redeploying.
Output: migration 0007_hardening.sql, updated circuit_breaker.py with daily halt, new resume_flow.py handler, build_telegram_app wires the command, daily_pipeline_job guards on daily halt.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hardening/07-CONTEXT.md
@.planning/phases/07-hardening/07-RESEARCH.md

@migrations/0006_analytics.sql
@src/app/services/circuit_breaker.py
@src/app/scheduler/jobs/cb_reset.py
@src/app/telegram/app.py
@src/app/telegram/handlers/mood_flow.py
@src/app/scheduler/jobs/daily_pipeline.py
</context>

<interfaces>
From src/app/services/circuit_breaker.py:
```python
class CircuitBreakerService:
    def __init__(self, supabase: Client) -> None: ...
    def get_state(self) -> dict: ...
    def is_tripped(self) -> bool: ...
    def record_attempt(self, cost_usd: float = 0.0) -> bool: ...
    def _trip(self, state: dict, final_cost: float, final_attempts: int) -> None: ...
    def _send_escalation_alert(self, trip_count: int) -> None: ...
    def midnight_reset(self) -> None: ...
```

From src/app/telegram/handlers/mood_flow.py (registration pattern to mirror):
```python
def register_mood_handlers(app: Application) -> None:
    creator_filter = get_creator_filter()
    app.add_handler(CommandHandler("resume", handle_resume, filters=creator_filter))
```

From src/app/telegram/app.py:
```python
def build_telegram_app() -> Application:
    # builds app and registers all handlers
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0007 + CircuitBreakerService daily halt extension + midnight_reset</name>
  <files>migrations/0007_hardening.sql, src/app/services/circuit_breaker.py, src/app/scheduler/jobs/cb_reset.py</files>
  <action>
    READ migrations/0003_video_columns.sql first to confirm all existing video_status values before writing the new CHECK constraint.

    Create migrations/0007_hardening.sql:
    - ALTER TABLE circuit_breaker_state ADD COLUMN IF NOT EXISTS daily_trip_count INTEGER NOT NULL DEFAULT 0
    - ALTER TABLE circuit_breaker_state ADD COLUMN IF NOT EXISTS daily_halted_at TIMESTAMPTZ
    - DROP CONSTRAINT IF EXISTS content_history_video_status_check
    - ADD CONSTRAINT content_history_video_status_check CHECK (video_status IN ('pending_render', 'pending_render_retry', 'rendering', 'processing', 'ready', 'failed', 'published', 'approval_timeout'))
    Include a comment header identifying this as Migration 0007 for Phase 7.

    Modify src/app/services/circuit_breaker.py:
    1. In _trip(): after the existing weekly_trip_count/is_tripped update, read state["daily_trip_count"] (default 0), increment it, update circuit_breaker_state.daily_trip_count in DB. Then if new_daily_trip_count >= 3 AND state does NOT already have daily_halted_at set: set daily_halted_at = now() in DB and call self._send_daily_halt_alert().
    2. Add method is_daily_halted(self) -> bool: query circuit_breaker_state for daily_halted_at IS NOT NULL. Return False on any exception (fail-open).
    3. Add method clear_daily_halt(self) -> None: update circuit_breaker_state with daily_trip_count=0, daily_halted_at=None, updated_at=now().
    4. Add method _send_daily_halt_alert(self) -> None: calls send_alert_sync with message "ALERTA: El pipeline ha sido detenido automaticamente despues de 3 intentos fallidos hoy. Para reanudar, escribe /resume en el bot." Log WARNING with extra pipeline_step="circuit_breaker_halt", content_history_id="".

    Modify src/app/scheduler/jobs/cb_reset.py midnight_reset logic:
    After existing weekly_trip_count and is_tripped reset, add clearing of daily halt state. Add daily_trip_count=0 and daily_halted_at=None to the existing UPDATE call (or add a second UPDATE if the first is for different columns). Read the current cb_reset.py to see exact update patterns before modifying.
  </action>
  <verify>
    <automated>cd /Users/jesusalbino/Projects/content-creation && python -c "import sys; sys.path.insert(0,'src'); from app.services.circuit_breaker import CircuitBreakerService; cb = object.__new__(CircuitBreakerService); assert hasattr(cb,'is_daily_halted'); assert hasattr(cb,'clear_daily_halt'); assert hasattr(cb,'_send_daily_halt_alert'); print('CircuitBreakerService OK')" 2>&1</automated>
  </verify>
  <done>
    - migrations/0007_hardening.sql exists with all four ALTER statements
    - CircuitBreakerService._trip() increments daily_trip_count and fires halt alert at >= 3 trips
    - CircuitBreakerService.is_daily_halted(), clear_daily_halt(), _send_daily_halt_alert() importable
    - cb_reset midnight_reset clears daily_trip_count=0 and daily_halted_at=NULL
  </done>
</task>

<task type="auto">
  <name>Task 2: /resume handler + wire into build_telegram_app + daily_pipeline_job guard</name>
  <files>src/app/telegram/handlers/resume_flow.py, src/app/telegram/app.py, src/app/scheduler/jobs/daily_pipeline.py</files>
  <action>
    Create src/app/telegram/handlers/resume_flow.py:
    - Module docstring: "Creator-only /resume command handler — clears circuit breaker daily halt and triggers immediate pipeline rerun."
    - Module-level logger = logging.getLogger(__name__)
    - async def handle_resume(update: Update, context: ContextTypes.DEFAULT_TYPE) -> None:
      - All DB/service imports inside the function body (lazy imports to avoid circular import chain)
      - Import get_supabase, CircuitBreakerService, trigger_immediate_rerun inside function
      - Call cb.clear_daily_halt()
      - Call trigger_immediate_rerun()
      - Log info with extra pipeline_step="manual_resume", content_history_id=""
      - No confirmation message to creator (CONTEXT.md locked decision: "no confirmation message")
    - def register_resume_handler(app: Application) -> None:
      - creator_filter = get_creator_filter()
      - app.add_handler(CommandHandler("resume", handle_resume, filters=creator_filter))
      - logger.info("Resume handler registered.")

    Modify src/app/telegram/app.py build_telegram_app():
    - At the end of the function (after existing register_* calls), add a lazy import and call:
      from app.telegram.handlers.resume_flow import register_resume_handler
      register_resume_handler(application)
    - Read the current build_telegram_app() to see the existing pattern and append accordingly.

    Modify src/app/scheduler/jobs/daily_pipeline.py daily_pipeline_job():
    - After the existing `if cb.is_tripped(): ... return` block, add:
      if cb.is_daily_halted():
          logger.warning("daily_pipeline_job: circuit breaker daily halt active — skipping. Send /resume to unblock.", extra={"pipeline_step": "pipeline_halt_check", "content_history_id": ""})
          return
    - Read the current daily_pipeline_job() to locate the exact insertion point (after is_tripped() block).
  </action>
  <verify>
    <automated>cd /Users/jesusalbino/Projects/content-creation && python -c "import sys; sys.path.insert(0,'src'); from app.telegram.handlers.resume_flow import handle_resume, register_resume_handler; from app.telegram.app import build_telegram_app; from app.scheduler.jobs.daily_pipeline import daily_pipeline_job; import inspect; src = inspect.getsource(daily_pipeline_job); assert 'is_daily_halted' in src; print('All wiring checks OK')" 2>&1</automated>
  </verify>
  <done>
    - resume_flow.py importable with handle_resume and register_resume_handler
    - build_telegram_app() calls register_resume_handler — /resume command active in bot
    - daily_pipeline_job() returns early if cb.is_daily_halted() is True
    - /resume is filtered to creator ID only (via get_creator_filter())
    - No circular imports in the import chain
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_phase04_smoke.py tests/test_phase05_smoke.py -v` — all existing smoke tests pass
- `python -c "from app.services.circuit_breaker import CircuitBreakerService; print('CB OK')"` — imports clean
- `python -c "from app.telegram.handlers.resume_flow import handle_resume, register_resume_handler; print('OK')"` — handler importable
- `python -c "from app.telegram.app import build_telegram_app; print('OK')"` — app buildable
- migrations/0007_hardening.sql contains daily_trip_count and approval_timeout constraint
</verification>

<success_criteria>
- Migration 0007 adds both daily halt columns and the approval_timeout status to content_history CHECK
- Circuit breaker sends a halt alert with /resume instructions after 3 daily trips — does not loop
- midnight_reset clears daily halt state for the next calendar day
- /resume command registered, creator-ID-only, triggers immediate pipeline rerun on invocation
- daily_pipeline_job returns early if daily halt is active
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening/07-03-SUMMARY.md`
</output>
