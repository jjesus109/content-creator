---
phase: 07-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_phase07_e2e.py
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - SCRTY-01
  - SCRTY-02
  - SCRP-01
  - SCRP-02
  - SCRP-03
  - SCRP-04
  - VIDP-01
  - VIDP-02
  - VIDP-03
  - VIDP-04
  - TGAP-01
  - TGAP-02
  - TGAP-03
  - TGAP-04
  - PUBL-01
  - PUBL-02
  - PUBL-03
  - PUBL-04
  - ANLX-01
  - ANLX-02
  - ANLX-03
  - ANLX-04

must_haves:
  truths:
    - "Running daily_pipeline_job() produces a content_history DB row with heygen_job_id, script_text, and video_status=pending_render"
    - "The mock Telegram send_approval_message_sync is called once after pipeline completes"
    - "HeyGen, Ayrshare, and Telegram are never called with real HTTP — all mocked at correct module paths"
    - "Test is skipped gracefully when ANTHROPIC_API_KEY is not set (not a hard test failure)"
  artifacts:
    - path: "tests/test_phase07_e2e.py"
      provides: "End-to-end integration test for the full pipeline chain"
      exports: ["test_daily_pipeline_writes_content_history"]
  key_links:
    - from: "tests/test_phase07_e2e.py"
      to: "app.scheduler.jobs.daily_pipeline.daily_pipeline_job"
      via: "direct function call (not APScheduler)"
      pattern: "daily_pipeline_job()"
    - from: "mock_all_externals fixture"
      to: "app.services.heygen.HeyGenService"
      via: "unittest.mock.patch at source module path"
      pattern: "patch.*app.services.heygen.HeyGenService"
---

<objective>
Create the end-to-end integration test that verifies all 26 v1 requirements run through a complete pipeline call.

Purpose: Provide the regression safety net for Phase 7 hardening changes. Catches broken imports, contract violations, and pipeline failures before they reach production.
Output: tests/test_phase07_e2e.py — a pytest file that calls daily_pipeline_job() with real Anthropic API and mocked HeyGen/Ayrshare/Telegram, asserting DB row creation and mock invocations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hardening/07-CONTEXT.md
@.planning/phases/07-hardening/07-RESEARCH.md

@tests/test_phase05_smoke.py
@src/app/scheduler/jobs/daily_pipeline.py
@src/app/services/telegram.py
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From src/app/scheduler/jobs/daily_pipeline.py:
```python
def daily_pipeline_job() -> None:
    """Top-level APScheduler job — call directly in tests (no scheduler needed)."""

def trigger_immediate_rerun() -> None:
    """Schedule daily_pipeline_job via DateTrigger 30s from now."""
```

From src/app/services/telegram.py:
```python
def send_alert_sync(message: str) -> None: ...
def send_approval_message_sync(content_history_id: str, video_url: str) -> None: ...
def get_creator_filter(): ...  # returns telegram filters object
```

Mock patch targets (patch at definition module, NOT importer module):
- HeyGenService (lazy import in daily_pipeline body): `app.services.heygen.HeyGenService`
- register_video_poller (lazy import in daily_pipeline body): `app.scheduler.jobs.video_poller.register_video_poller`
- send_alert_sync (top-level import): `app.services.telegram.send_alert_sync`
- send_approval_message_sync (top-level import): `app.services.telegram.send_approval_message_sync`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write E2E integration test with mocked externals</name>
  <files>tests/test_phase07_e2e.py</files>
  <behavior>
    - test_daily_pipeline_writes_content_history: calls daily_pipeline_job(), asserts content_history row exists with heygen_job_id == "fake-heygen-job-id-e2e", video_status == "pending_render", and non-empty script_text
    - mock_all_externals fixture: patches HeyGenService.submit to return "fake-heygen-job-id-e2e", mocks register_video_poller, send_alert_sync, and send_approval_message_sync
    - test is decorated with @pytest.mark.e2e and skipped if ANTHROPIC_API_KEY env var is not set (use pytest.importorskip or pytest.mark.skipif)
    - after daily_pipeline_job() call, send_approval_message_sync mock is asserted as called_once (Telegram delivery happened)
    - HeyGen mock submit is asserted as called_once (HeyGen submission happened)
  </behavior>
  <action>
    Create tests/test_phase07_e2e.py.

    File structure:
    1. Module docstring explaining: real Anthropic API, mocked HeyGen/Ayrshare/Telegram, direct daily_pipeline_job() call (no APScheduler).
    2. sys.path.insert to add src/ (same pattern as test_phase05_smoke.py).
    3. pytestmark = pytest.mark.e2e at module level.
    4. autouse fixture `clear_lru_cache` that calls get_settings.cache_clear() after each test — prevents stale settings between tests (Pitfall 7 from RESEARCH.md).
    5. Skip decorator on test: `@pytest.mark.skipif(not os.getenv("ANTHROPIC_API_KEY"), reason="ANTHROPIC_API_KEY not set — E2E skipped")`.
    6. `mock_all_externals` fixture using context manager form of patch:
       - `patch("app.services.heygen.HeyGenService")` — configure instance.submit.return_value = "fake-heygen-job-id-e2e"
       - `patch("app.scheduler.jobs.video_poller.register_video_poller")` — no-op mock
       - `patch("app.services.telegram.send_alert_sync")` — capture calls
       - `patch("app.services.telegram.send_approval_message_sync")` — capture calls
       - yield a dict of the mocks so assertions can reference them
    7. `test_daily_pipeline_writes_content_history(mock_all_externals)`:
       - import daily_pipeline_job inside the test function body
       - import get_supabase inside the test function body
       - call daily_pipeline_job()
       - query content_history: select("*").order("created_at", desc=True).limit(1).execute()
       - assert rows.data is not empty
       - assert row["heygen_job_id"] == "fake-heygen-job-id-e2e"
       - assert row["video_status"] == "pending_render"
       - assert row["script_text"] — non-empty string
       - assert mock_all_externals["heygen"].submit.call_count == 1
       - assert mock_all_externals["approval"].call_count == 1

    IMPORTANT: Do NOT mock Anthropic API — real script generation must run (locked decision).
    IMPORTANT: Patch at the source module path (app.services.heygen.HeyGenService), not at the import site inside daily_pipeline_job body. For lazy imports inside the function body, the source module is the correct patch target — see RESEARCH.md Pitfall 1 for detailed explanation.
    IMPORTANT: Do NOT call get_settings() at module import time — only inside test functions after sys.path is set, to avoid ValidationError from missing env vars.
  </action>
  <verify>
    <automated>cd /Users/jesusalbino/Projects/content-creation && python -m pytest tests/test_phase07_e2e.py -v --collect-only 2>&1 | head -20</automated>
  </verify>
  <done>
    - tests/test_phase07_e2e.py exists and is importable by pytest
    - pytest --collect-only shows test_daily_pipeline_writes_content_history collected under e2e mark
    - Running without ANTHROPIC_API_KEY produces SKIPPED (not FAILED)
    - When ANTHROPIC_API_KEY is set: test calls daily_pipeline_job(), asserts content_history row and mock invocations
  </done>
</task>

</tasks>

<verification>
- `cd /Users/jesusalbino/Projects/content-creation && python -m pytest tests/test_phase07_e2e.py -v --collect-only` — test collected, no import errors
- `cd /Users/jesusalbino/Projects/content-creation && python -m pytest tests/test_phase07_e2e.py -v -m e2e` — skips gracefully if ANTHROPIC_API_KEY not set
- All existing smoke tests still pass: `python -m pytest tests/test_phase04_smoke.py tests/test_phase05_smoke.py -v`
</verification>

<success_criteria>
- tests/test_phase07_e2e.py is collected by pytest with no import errors
- Test skips when ANTHROPIC_API_KEY is absent (not fails)
- When run with real credentials: content_history row is written with correct fields
- Mocks prevent any real HeyGen/Telegram HTTP calls during test
- Existing smoke tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening/07-01-SUMMARY.md`
</output>
