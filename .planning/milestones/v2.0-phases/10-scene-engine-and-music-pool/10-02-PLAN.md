---
phase: 10-scene-engine-and-music-pool
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/data/scenes.json
  - src/app/services/scene_generation.py
  - tests/test_scene_engine.py
  - tests/test_seasonal_calendar.py
  - tests/test_caption_generator.py
autonomous: true
requirements:
  - SCN-01
  - SCN-02
  - SCN-05

must_haves:
  truths:
    - "scenes.json contains 40-60 explicit {location, activity, mood, weight} tuples"
    - "SceneEngine.pick_scene() returns (scene_prompt, caption, mood, cost_usd) from a single GPT-4o call"
    - "The 5-8 word Spanish caption is returned in the same GPT-4o response as the scene prompt"
    - "Seasonal overlays for Sep 16, Nov 1-2, Nov 20, Aug 8 inject themed context into the GPT-4o call"
    - "Non-seasonal dates produce no overlay"
    - "Scene library is loaded once at module init, not per call"
  artifacts:
    - path: "src/app/data/scenes.json"
      provides: "40-60 curated scene combo tuples (location + activity + mood + weight)"
      min_lines: 42
    - path: "src/app/services/scene_generation.py"
      provides: "SceneEngine class with pick_scene() and SeasonalCalendarService"
      exports: ["SceneEngine", "SeasonalCalendarService"]
  key_links:
    - from: "src/app/services/scene_generation.py"
      to: "src/app/data/scenes.json"
      via: "json.load() at __init__ time"
      pattern: "scenes\\.json"
    - from: "src/app/services/scene_generation.py"
      to: "OpenAI GPT-4o"
      via: "client.chat.completions.create(model='gpt-4o', response_format={'type': 'json_object'})"
      pattern: "gpt-4o"
---

<objective>
Create the scene library JSON and SceneEngine service (GPT-4o scene selection, caption generation, and seasonal calendar overlay).

Purpose: SceneEngine is the core of v2.0 content generation — it replaces ScriptGenerationService entirely. Every daily run starts here.

Output: scenes.json (40-60 curated combos), scene_generation.py (SceneEngine + SeasonalCalendarService), and activated test coverage for SCN-01, SCN-02, SCN-05.
</objective>

<execution_context>
@/Users/jesusalbino/Projects/content-creation/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jesusalbino/Projects/content-creation/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-scene-engine-and-music-pool/10-CONTEXT.md
@.planning/phases/10-scene-engine-and-music-pool/10-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor MUST follow. Extracted from codebase. -->

From src/app/services/script_generation.py (pattern to mirror for SceneEngine):
```python
class ScriptGenerationService:
    def __init__(self, supabase: Client | None = None) -> None:
        self._supabase = supabase or get_supabase()
        settings = get_settings()
        self._client = Anthropic(api_key=settings.anthropic_api_key)
        self._model = settings.claude_generation_model

    def load_active_rejection_constraints(self) -> list[dict]:
        result = self._supabase.table("rejection_constraints").select(
            "reason_text, pattern_type"
        ).gt("expires_at", "now()").execute()
        return result.data or []
```
KEY DIFFERENCE: SceneEngine uses OpenAI (not Anthropic) — GPT-4o via synchronous OpenAI client.

From src/app/services/embeddings.py (same client pattern):
```python
from openai import OpenAI
class EmbeddingService:
    def __init__(self) -> None:
        settings = get_settings()
        self._client = OpenAI(api_key=settings.openai_api_key)
```

From src/app/settings.py (settings to use):
```python
class Settings(BaseSettings):
    openai_api_key: str
    # SceneEngine will read openai_api_key — no new settings needed for this plan
```

GPT-4o cost rates (for cost tracking):
- Input: $2.50/MTok (gpt-4o)
- Output: $10.00/MTok (gpt-4o)
- Use these in cost calculation: (input_tokens * 2.50 + output_tokens * 10.00) / 1_000_000

CHARACTER_BIBLE constant from src/app/services/kling.py:
```python
CHARACTER_BIBLE = (
    "Orange tabby named Mochi, green eyes, white chest patch, "
    "slightly chubby, curious personality. Lives in a warm Mexican household. "
    "Natural domestic cat behavior — no costumes, no anthropomorphism. "
    "Consistent warm orange fur with darker tabby stripes."
)
```
Import CHARACTER_BIBLE from kling.py — do NOT redefine it in scene_generation.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scenes.json — 40-60 curated scene combos</name>
  <files>src/app/data/scenes.json</files>
  <read_first>
    - .planning/phases/10-scene-engine-and-music-pool/10-CONTEXT.md (JSON structure locked decision)
    - .planning/phases/10-scene-engine-and-music-pool/10-RESEARCH.md (Pattern 1: scene library JSON structure example)
    - src/app/data/music_seed.csv (if it exists — check that src/app/data/ dir exists)
  </read_first>
  <behavior>
    - Each entry is {"location": str, "activity": str, "mood": str, "weight": float}
    - Mood values must be exactly: "playful", "sleepy", or "curious" (no other values)
    - Weight is optional but recommended: 1.0 = standard, 0.5-0.8 = less frequent, 1.2-1.5 = preferred
    - Minimum 40 entries, target 50-60
    - Variety: at least 10 entries per mood (playful/sleepy/curious)
    - Locations are real domestic Mexican household spaces (cocina, sala, terraza, jardín, baño, recámara, balcón, patio)
    - Activities are naturalistic cat behaviors (not anthropomorphic)
    - Spanish for locations/activities consistent with Mexican household context
  </behavior>
  <action>
Create `src/app/data/scenes.json` as a JSON array. The file MUST have at least 50 entries. Use exactly this structure per entry: `{"location": "...", "activity": "...", "mood": "...", "weight": 1.0}`.

Include entries from ALL THREE moods. Aim for 18 curious, 16 playful, 16 sleepy entries (total = 50).

Example entries for each mood:

CURIOUS (mood = "curious", BPM matching: 90-100):
- cocina / inspeccionar olla humeante / curious
- jardín / olfatear planta desconocida / curious
- ventana / seguir vuelo de pájaro / curious
- recámara / explorar bolsa nueva / curious
- baño / observar agua del grifo / curious
- sala / investigar caja de cartón / curious
- terraza / estudiar hormiga en el suelo / curious
- patio / examinar flor caída / curious
- cocina / vigilar licuadora encendida / curious
- balcón / mirar vecino desde arriba / curious
- recámara / explorar closet abierto / curious
- jardín / descubrir hoyo en la tierra / curious
- sala / investigar bolsa de compras / curious
- baño / curiosear detrás del espejo / curious
- terraza / observar paloma en el techo / curious
- cocina / seguir movimiento de las llamas / curious
- patio / explorar rincón oscuro / curious
- jardín / olfatear zapatos nuevos / curious

PLAYFUL (mood = "playful", BPM matching: 110-125):
- sala / perseguir juguete de plumas / playful
- jardín / cazar mariposa / playful
- terraza / saltar sobre sombra / playful
- recámara / atacar pie debajo del cobertor / playful
- sala / deslizarse en piso de madera / playful
- patio / correr en círculos / playful
- jardín / trepar árbol bajo / playful
- cocina / perseguir tapón de botella / playful
- terraza / brincar en macetas / playful
- sala / esconderse detrás de cortina / playful
- recámara / rodar con pelota de papel / playful
- patio / emboscar desde arbustos / playful
- balcón / atrapar mosca / playful
- jardín / correr tras hoja voladora / playful
- sala / jugar con bolsa de plástico / playful
- terraza / perseguir su propia cola / playful

SLEEPY (mood = "sleepy", BPM matching: 70-80):
- sofá / dormir estirado al sol / sleepy
- recámara / acurrucarse en almohada / sleepy
- terraza / tomar sol de la tarde / sleepy
- sala / dormir en rincón acogedor / sleepy
- jardín / sestear bajo la sombra / sleepy
- balcón / descansar mirando el horizonte / sleepy
- recámara / ronronear en cama / sleepy
- sofá / dormitar con ojo entreabierto / sleepy
- patio / descansar sobre baldosas tibias / sleepy
- jardín / echarse en el pasto / sleepy
- sala / acomodarse en canasta / sleepy
- recámara / estirarse al despertar / sleepy
- terraza / parpadear lentamente al sol / sleepy
- sofá / enrollarse en cobija / sleepy
- balcón / descansar con viento suave / sleepy
- jardín / tomar la siesta vespertina / sleepy
  </action>
  <verify>
    <automated>python3 -c "import json; data = json.load(open('src/app/data/scenes.json')); moods = [e['mood'] for e in data]; print(f'Total: {len(data)}, curious: {moods.count(\"curious\")}, playful: {moods.count(\"playful\")}, sleepy: {moods.count(\"sleepy\")}'); assert len(data) >= 40, f'Need 40+ entries, got {len(data)}'; assert all(e[\"mood\"] in {\"playful\",\"sleepy\",\"curious\"} for e in data), 'Invalid mood found'"</automated>
  </verify>
  <done>
    - src/app/data/scenes.json is valid JSON
    - Contains at least 40 entries (target 50)
    - All entries have "location", "activity", "mood", "weight" keys
    - All mood values are exactly "playful", "sleepy", or "curious"
    - At least 10 entries per mood
    - python3 validation command above exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SceneEngine service (GPT-4o scene selection + caption + seasonal calendar)</name>
  <files>
    src/app/services/scene_generation.py,
    tests/test_scene_engine.py,
    tests/test_seasonal_calendar.py,
    tests/test_caption_generator.py
  </files>
  <read_first>
    - src/app/services/script_generation.py (ScriptGenerationService pattern to mirror: __init__, sync OpenAI client, cost tracking, rejection constraints pattern)
    - src/app/services/embeddings.py (OpenAI sync client instantiation pattern)
    - src/app/services/kling.py (CHARACTER_BIBLE constant — import it, don't redefine)
    - src/app/settings.py (get_settings() pattern, openai_api_key field)
    - src/app/services/database.py (get_supabase() pattern)
    - tests/conftest.py (mock_scene_library, mock_openai_client, mock_supabase fixtures)
    - src/app/data/scenes.json (the scene library to load)
  </read_first>
  <behavior>
    - SceneEngine.__init__: loads scenes.json at init (not per call), instantiates OpenAI sync client
    - SceneEngine.pick_scene(attempt=0, rejection_constraints=None) returns tuple[str, str, str, float] = (scene_prompt, caption, mood, cost_usd)
    - scene_prompt: 2-3 sentences, Kling-optimized, includes CHARACTER_BIBLE context
    - caption: 5-8 words Spanish, [observation] + [implied personality] formula
    - Single GPT-4o call returns both in JSON: {"scene_prompt": "...", "caption": "..."}
    - mood: returned from the selected scene combo (not generated by GPT-4o)
    - cost_usd: calculated from token usage (input * 2.50 + output * 10.00) / 1_000_000
    - SeasonalCalendarService.get_overlay(date) returns str overlay text or None
    - When overlay exists, it's injected as additional context into the GPT-4o system prompt
    - load_active_scene_rejections() loads rejection_constraints WHERE pattern_type='scene' AND expires_at > now()
    - store_scene_rejection(scene_combo, reason_text) inserts with expires_at = now() + 7 days
  </behavior>
  <action>
Create `src/app/services/scene_generation.py` with the following exact implementation:

```python
import json
import logging
import random
from datetime import date, datetime, timedelta, timezone
from pathlib import Path
from typing import Optional

from openai import OpenAI
from supabase import Client

from app.services.database import get_supabase
from app.services.kling import CHARACTER_BIBLE
from app.settings import get_settings

logger = logging.getLogger(__name__)

# GPT-4o cost rates (per 1M tokens)
GPT4O_COST_INPUT_PER_MTOK = 2.50
GPT4O_COST_OUTPUT_PER_MTOK = 10.00

# Absolute path to scenes.json (resolved from this file's location)
SCENES_JSON_PATH = Path(__file__).resolve().parents[1] / "data" / "scenes.json"

# Seasonal calendar: (month, day) -> (theme_name, overlay_text)
SEASONAL_OVERLAYS: dict[tuple[int, int], tuple[str, str]] = {
    (9, 16): (
        "Día de Independencia",
        "Hoy es el Día de Independencia de México (16 de septiembre). "
        "Incorpora sutilmente elementos de celebración mexicana en la escena — "
        "por ejemplo, una bandera verde-blanco-rojo visible al fondo, o luces de colores. "
        "No cambies la actividad del gato; solo añade ambiente festivo patriótico."
    ),
    (11, 1): (
        "Día de Muertos",
        "Hoy comienza el Día de Muertos (1 de noviembre). "
        "Añade una sutil referencia visual a la tradición — cempasúchil cerca, velas encendidas, "
        "o altar de fondo. Mantén el comportamiento natural del gato; el ambiente es el que cambia."
    ),
    (11, 2): (
        "Día de Muertos",
        "Hoy es el segundo día del Día de Muertos (2 de noviembre). "
        "Añade una sutil referencia visual a la tradición — cempasúchil cerca, velas encendidas, "
        "o altar de fondo. Mantén el comportamiento natural del gato; el ambiente es el que cambia."
    ),
    (11, 20): (
        "Día de la Revolución",
        "Hoy es el Día de la Revolución Mexicana (20 de noviembre). "
        "Añade elementos sutiles de orgullo mexicano en el ambiente — colores de la bandera, "
        "decoración tricolor al fondo. El comportamiento del gato no cambia."
    ),
    (8, 8): (
        "Día Internacional del Gato",
        "Hoy es el Día Internacional del Gato (8 de agosto). "
        "Haz que la escena celebre especialmente a Mochi — tal vez con un lazo especial, "
        "o mostrando su mejor lado. El ambiente puede ser un poco más festivo de lo normal."
    ),
}


class SeasonalCalendarService:
    """
    Checks if today matches a seasonal holiday and returns a themed overlay string.
    Overlay is injected into the GPT-4o prompt as additional context.
    """

    def get_overlay(self, check_date: Optional[date] = None) -> Optional[str]:
        """
        Returns overlay text for the given date (defaults to today in Mexico City time).
        Returns None if no holiday matches.
        """
        if check_date is None:
            # Use Mexico City timezone for date calculation
            import pytz
            mexico_tz = pytz.timezone("America/Mexico_City")
            check_date = datetime.now(tz=mexico_tz).date()

        key = (check_date.month, check_date.day)
        if key in SEASONAL_OVERLAYS:
            theme_name, overlay_text = SEASONAL_OVERLAYS[key]
            logger.info("Seasonal overlay active: %s (%s)", theme_name, check_date.isoformat())
            return overlay_text
        return None


class SceneEngine:
    """
    Generates daily scene prompts via GPT-4o from a curated scene library.

    Single API call returns both:
    - expanded scene prompt (2-3 sentences for Kling generation)
    - 5-8 word Spanish caption ([observation] + [implied personality] formula)

    Scene library (scenes.json) loaded ONCE at init — not per call.
    All OpenAI API calls use the synchronous client (APScheduler ThreadPoolExecutor).
    Cost is returned for circuit breaker tracking.
    """

    def __init__(self, supabase: Client | None = None) -> None:
        self._supabase = supabase or get_supabase()
        settings = get_settings()
        self._client = OpenAI(api_key=settings.openai_api_key)
        self._scene_library = self._load_scene_library()
        self._seasonal = SeasonalCalendarService()
        logger.info(
            "SceneEngine initialized: %d scenes loaded from %s",
            len(self._scene_library),
            SCENES_JSON_PATH,
        )

    def _load_scene_library(self) -> list[dict]:
        """Load scenes.json once at init. Raises FileNotFoundError if missing."""
        with open(SCENES_JSON_PATH, encoding="utf-8") as f:
            library = json.load(f)
        if not library:
            raise ValueError("scenes.json is empty — cannot generate scenes")
        return library

    def _select_combo(self) -> dict:
        """
        Weighted random selection from scene library.
        Uses weight field if present; defaults to 1.0 if absent.
        """
        weights = [entry.get("weight", 1.0) for entry in self._scene_library]
        return random.choices(self._scene_library, weights=weights, k=1)[0]

    def _build_system_prompt(
        self,
        combo: dict,
        rejection_constraints: list[dict] | None = None,
        seasonal_overlay: Optional[str] = None,
    ) -> str:
        """Build the GPT-4o system prompt for scene + caption generation."""
        rejection_text = ""
        if rejection_constraints:
            rejection_lines = ["\\nEVITA estas escenas (rechazadas por el creador):"]
            for r in rejection_constraints:
                combo_info = r.get("scene_combo") or {}
                reason = r.get("reason_text", "sin razón")
                if combo_info:
                    rejection_lines.append(
                        f"- {combo_info.get('location', '?')} / {combo_info.get('activity', '?')}: {reason}"
                    )
            rejection_text = "\\n".join(rejection_lines)

        seasonal_text = ""
        if seasonal_overlay:
            seasonal_text = f"\\n\\nCONTEXTO ESTACIONAL ESPECIAL:\\n{seasonal_overlay}"

        return f"""Eres un director creativo para producción de videos de gato.
Se te da una composición de escena (location + activity + mood). Tu tarea:
1. Expandirla en un prompt visual vívido de 2-3 oraciones para generación de video con IA.
2. Generar un pie de foto en español de 5-8 palabras siguiendo la fórmula: [observación] + [personalidad implícita].

PERSONAJE — Character Bible (incluir en el prompt):
{CHARACTER_BIBLE}

ESCENA SELECCIONADA:
- Ubicación: {combo['location']}
- Actividad: {combo['activity']}
- Estado de ánimo: {combo['mood']}
{seasonal_text}
{rejection_text}

INSTRUCCIONES PARA EL PROMPT DE ESCENA:
- 2-3 oraciones completas, describen la escena visualmente para generación de video
- Incluir el nombre "Mochi" y referencia al Character Bible
- Describir la acción, el ambiente, la luz y el ánimo
- Optimizado para Kling AI (video generación): específico, visual, cinematográfico

INSTRUCCIONES PARA EL PIE DE FOTO:
- Exactamente 5-8 palabras en español
- Fórmula: [lo que hace/observa] + [personalidad del gato implícita]
- Ejemplos: "Mochi descubre los secretos de la cocina", "El gato vigila su territorio con calma"
- NO usar hashtags, NO usar emojis

Devuelve ÚNICAMENTE un JSON válido con exactamente estas dos claves:
{{"scene_prompt": "...", "caption": "..."}}"""

    def pick_scene(
        self,
        attempt: int = 0,
        rejection_constraints: list[dict] | None = None,
    ) -> tuple[str, str, str, float]:
        """
        Generate a scene prompt and caption for today's video.

        Returns:
            (scene_prompt, caption, mood, cost_usd)
            - scene_prompt: 2-3 sentence Kling-optimized prompt
            - caption: 5-8 word Spanish caption
            - mood: "playful" | "sleepy" | "curious" (from selected combo)
            - cost_usd: GPT-4o API cost for circuit breaker tracking

        attempt: 0 = normal; 1+ = retry with different combo (similarity rejection)
        """
        combo = self._select_combo()
        seasonal_overlay = self._seasonal.get_overlay()

        system_prompt = self._build_system_prompt(
            combo=combo,
            rejection_constraints=rejection_constraints,
            seasonal_overlay=seasonal_overlay,
        )

        logger.info(
            "SceneEngine picking scene (attempt %d): location=%s, activity=%s, mood=%s",
            attempt + 1, combo["location"], combo["activity"], combo["mood"],
        )

        try:
            response = self._client.chat.completions.create(
                model="gpt-4o",
                messages=[{"role": "user", "content": system_prompt}],
                response_format={"type": "json_object"},
                temperature=0.85,
                max_tokens=400,
            )
        except Exception as e:
            logger.error("GPT-4o scene generation failed: %s", e)
            raise

        raw_content = response.choices[0].message.content
        try:
            parsed = json.loads(raw_content)
            scene_prompt = parsed["scene_prompt"].strip()
            caption = parsed["caption"].strip()
        except (json.JSONDecodeError, KeyError) as e:
            logger.error("GPT-4o returned invalid JSON for scene: %s | raw: %s", e, raw_content)
            raise ValueError(f"SceneEngine: invalid GPT-4o response structure: {e}") from e

        # Calculate cost
        input_tokens = response.usage.prompt_tokens
        output_tokens = response.usage.completion_tokens
        cost_usd = (
            input_tokens * GPT4O_COST_INPUT_PER_MTOK
            + output_tokens * GPT4O_COST_OUTPUT_PER_MTOK
        ) / 1_000_000

        logger.info(
            "Scene generated: mood=%s, caption='%s', cost=$%.6f",
            combo["mood"], caption, cost_usd,
        )

        return scene_prompt, caption, combo["mood"], cost_usd

    def load_active_scene_rejections(self) -> list[dict]:
        """
        Load active scene rejection constraints (expires_at > now()).
        Returns list of {scene_combo, reason_text}.
        """
        try:
            result = self._supabase.table("rejection_constraints").select(
                "scene_combo, reason_text"
            ).eq("pattern_type", "scene").gt("expires_at", "now()").execute()
            return result.data or []
        except Exception as e:
            logger.error("Failed to load scene rejections: %s — proceeding without", e)
            return []

    def store_scene_rejection(self, scene_combo: dict, reason_text: str) -> None:
        """
        Store a rejected scene combo with 7-day automatic expiry.
        Called when creator rejects a video (from approval_flow).
        """
        expires_at = (datetime.now(timezone.utc) + timedelta(days=7)).isoformat()
        try:
            self._supabase.table("rejection_constraints").insert({
                "reason_text": reason_text,
                "pattern_type": "scene",
                "scene_combo": scene_combo,
                "expires_at": expires_at,
            }).execute()
            logger.info(
                "Scene rejection stored: %s — expires %s", scene_combo, expires_at
            )
        except Exception as e:
            logger.error("Failed to store scene rejection: %s", e)
```

Then update `tests/test_scene_engine.py`, `tests/test_seasonal_calendar.py`, and `tests/test_caption_generator.py` to replace the skipped stubs with real tests using mocks:

**`tests/test_scene_engine.py`** (replace fully):
```python
"""Tests for SCN-01 (scene selection) and SCN-05 (Spanish caption)."""
import json
import pytest
from unittest.mock import MagicMock, patch

def test_scene_library_structure(mock_scene_library):
    """SCN-01: scene library entries have location, activity, mood fields."""
    required_fields = {"location", "activity", "mood"}
    for entry in mock_scene_library:
        assert required_fields.issubset(entry.keys())

def test_mood_values_valid(mock_scene_library):
    """SCN-01: mood is one of playful/sleepy/curious."""
    valid_moods = {"playful", "sleepy", "curious"}
    for entry in mock_scene_library:
        assert entry["mood"] in valid_moods

def test_scene_engine_init_loads_library(tmp_path, mock_scene_library):
    """SCN-01: SceneEngine loads scene library at init."""
    import json
    scenes_file = tmp_path / "scenes.json"
    scenes_file.write_text(json.dumps(mock_scene_library))

    with patch("app.services.scene_generation.SCENES_JSON_PATH", scenes_file), \
         patch("app.services.scene_generation.get_supabase"), \
         patch("app.services.scene_generation.get_settings") as mock_settings, \
         patch("app.services.scene_generation.OpenAI"):
        mock_settings.return_value.openai_api_key = "test-key"
        from app.services.scene_generation import SceneEngine
        engine = SceneEngine.__new__(SceneEngine)
        engine._scene_library = mock_scene_library
        assert len(engine._scene_library) == len(mock_scene_library)

def test_pick_scene_returns_correct_tuple(mock_scene_library, mock_openai_client):
    """SCN-01, SCN-05: pick_scene() returns (scene_prompt, caption, mood, cost)."""
    import json
    from unittest.mock import patch, MagicMock
    from app.services.scene_generation import SceneEngine

    with patch("app.services.scene_generation.get_supabase"), \
         patch("app.services.scene_generation.get_settings") as mock_settings, \
         patch("app.services.scene_generation.OpenAI", return_value=mock_openai_client), \
         patch("builtins.open", return_value=MagicMock(
             __enter__=lambda s: s,
             __exit__=MagicMock(return_value=False),
             read=lambda: json.dumps(mock_scene_library)
         )):
        mock_settings.return_value.openai_api_key = "test-key"
        engine = MagicMock(spec=SceneEngine)
        engine._client = mock_openai_client
        engine._scene_library = mock_scene_library
        engine._seasonal = MagicMock()
        engine._seasonal.get_overlay.return_value = None
        engine._select_combo = lambda: mock_scene_library[0]
        engine._build_system_prompt = SceneEngine._build_system_prompt.__get__(engine)
        engine.pick_scene = SceneEngine.pick_scene.__get__(engine)
        scene_prompt, caption, mood, cost = engine.pick_scene()
        assert isinstance(scene_prompt, str) and len(scene_prompt) > 0
        assert isinstance(caption, str) and len(caption) > 0
        assert mood in {"playful", "sleepy", "curious"}
        assert isinstance(cost, float) and cost >= 0

def test_caption_word_count_fixture():
    """SCN-05: caption is between 5 and 8 words (fixture validation)."""
    captions = [
        "Mochi descubre los secretos de la cocina",
        "El gato observa la lluvia caer",
        "Mochi juega con su cola",
    ]
    for caption in captions:
        word_count = len(caption.split())
        assert 5 <= word_count <= 8, f"Caption '{caption}' has {word_count} words"
```

**`tests/test_seasonal_calendar.py`** (replace fully):
```python
"""Tests for SCN-02 (seasonal calendar overlays)."""
from datetime import date
import pytest
from unittest.mock import patch
from app.services.scene_generation import SeasonalCalendarService

def test_independence_day_overlay():
    """SCN-02: Sep 16 returns Día de Independencia overlay."""
    svc = SeasonalCalendarService()
    overlay = svc.get_overlay(date(2026, 9, 16))
    assert overlay is not None
    assert "Independencia" in overlay or "septiembre" in overlay or "16" in overlay

def test_day_of_dead_overlay_nov1():
    """SCN-02: Nov 1 returns Día de Muertos overlay."""
    svc = SeasonalCalendarService()
    overlay = svc.get_overlay(date(2026, 11, 1))
    assert overlay is not None
    assert "Muertos" in overlay or "noviembre" in overlay

def test_day_of_dead_overlay_nov2():
    """SCN-02: Nov 2 returns Día de Muertos overlay."""
    svc = SeasonalCalendarService()
    overlay = svc.get_overlay(date(2026, 11, 2))
    assert overlay is not None
    assert "Muertos" in overlay or "noviembre" in overlay

def test_revolution_day_overlay():
    """SCN-02: Nov 20 returns Día de la Revolución overlay."""
    svc = SeasonalCalendarService()
    overlay = svc.get_overlay(date(2026, 11, 20))
    assert overlay is not None
    assert "Revolución" in overlay or "noviembre" in overlay

def test_cat_day_overlay():
    """SCN-02: Aug 8 returns Día Internacional del Gato overlay."""
    svc = SeasonalCalendarService()
    overlay = svc.get_overlay(date(2026, 8, 8))
    assert overlay is not None
    assert "Gato" in overlay or "agosto" in overlay

def test_non_holiday_returns_none():
    """SCN-02: Non-holiday date returns None (no overlay)."""
    svc = SeasonalCalendarService()
    assert svc.get_overlay(date(2026, 3, 15)) is None
    assert svc.get_overlay(date(2026, 7, 4)) is None
    assert svc.get_overlay(date(2026, 12, 25)) is None
```

**`tests/test_caption_generator.py`** (replace fully):
```python
"""Tests for SCN-05 (Spanish caption formula)."""
import pytest

def test_caption_word_count():
    """SCN-05: caption is between 5 and 8 words."""
    captions = [
        "Mochi descubre los secretos de la cocina",
        "El gato observa la lluvia caer",
        "Mochi juega con su cola",
        "El gato vigila su territorio con calma",
    ]
    for caption in captions:
        word_count = len(caption.split())
        assert 5 <= word_count <= 8, f"Caption '{caption}' has {word_count} words"

def test_caption_not_empty():
    """SCN-05: caption must not be empty string."""
    caption = "Mochi descubre los secretos de la cocina"
    assert len(caption.strip()) > 0

def test_caption_no_hashtags():
    """SCN-05: caption must not contain hashtags."""
    captions = [
        "Mochi descubre los secretos de la cocina",
        "El gato observa la lluvia caer",
    ]
    for caption in captions:
        assert "#" not in caption, f"Caption contains hashtag: {caption}"
```
  </action>
  <verify>
    <automated>uv run pytest tests/test_scene_engine.py tests/test_seasonal_calendar.py tests/test_caption_generator.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    - src/app/services/scene_generation.py exists and contains: class SceneEngine, class SeasonalCalendarService, SEASONAL_OVERLAYS dict with 5 entries ((9,16), (11,1), (11,2), (11,20), (8,8)), pick_scene() method, load_active_scene_rejections(), store_scene_rejection()
    - grep confirms: "CHARACTER_BIBLE" import from app.services.kling in scene_generation.py
    - grep confirms: "gpt-4o" in scene_generation.py
    - grep confirms: "json_object" in scene_generation.py (response_format={"type": "json_object"})
    - src/app/data/scenes.json exists with 40+ entries
    - `uv run pytest tests/test_seasonal_calendar.py -v --tb=short` exits 0 with all 6 tests passing (no skips)
    - `uv run pytest tests/test_caption_generator.py -v --tb=short` exits 0 with all 3 tests passing
    - `uv run pytest tests/ -x -q --tb=short` exits 0 overall
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `python3 -c "import json; data=json.load(open('src/app/data/scenes.json')); assert len(data) >= 40"` exits 0
- `grep "SeasonalCalendarService" src/app/services/scene_generation.py` returns a match
- `grep "pick_scene" src/app/services/scene_generation.py` returns a match
- `grep "store_scene_rejection" src/app/services/scene_generation.py` returns a match
- `grep "gpt-4o" src/app/services/scene_generation.py` returns a match
- `uv run pytest tests/test_seasonal_calendar.py tests/test_caption_generator.py -v --tb=short` exits 0
- `uv run pytest tests/ -x -q --tb=short` exits 0
</verification>

<success_criteria>
- scenes.json has 40+ entries, all with valid moods (playful/sleepy/curious), min 10 per mood
- SceneEngine.pick_scene() signature is (attempt=0, rejection_constraints=None) -> tuple[str, str, str, float]
- SeasonalCalendarService.get_overlay() returns overlay text for all 5 holiday dates and None for all other dates
- All test_seasonal_calendar.py tests pass without skips (6 tests)
- All test_caption_generator.py tests pass without skips (3 tests)
- No import errors when running `python3 -c "from app.services.scene_generation import SceneEngine, SeasonalCalendarService"`
</success_criteria>

<output>
After completion, create `.planning/phases/10-scene-engine-and-music-pool/10-02-SUMMARY.md`
</output>
